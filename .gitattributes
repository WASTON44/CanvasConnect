# Keep repository text files consistent across Windows, macOS, and Linux.
* text=auto eol=lf

# Preserve binary teaching resources if a contributor intentionally adds a
# generic fixture in future; local course archives are ignored by default.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.docx binary
*.pptx binary
*.xlsx binary
